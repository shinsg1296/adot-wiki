---
type: project
date: 2026-07-04
status: active
owner: 원장
source: 2026-06-09~10(파이프라인 구축·144문항·picker) + 2026-06-12(부교재·모의고사 5유형·웹개선·신규4유형 분석) + 2026-06-13(메가스터디) + 2026-07-01~03(수특 Light영어 전권·무관오염 정리·배포) + 2026-07-04(전수 QA=결정적 게이트↔AI 스윕·INS_ADJ 39·순서라벨 게이트화). 코드=`C:\모의고사자동화\_hawe_q_*`·`_hq5_*`·`_stl_*`·`_haweq_*`·`_qa_gate.py`·`_qa/*`, 웹=`C:\하위큐웹`, 배포=verdant-unicorn/haweq
---

# 하위큐(HAWE Q) 문제 생성 시스템

## Summary

기출 **지문을 재료로 새 문제(현재 제목추론)를 자동 생성**하고, **웹 picker로 골라 맞춤 문제지·정답해설(docx/pdf)을 뽑는** 시스템. AINGEL식 다운로드 자료의 자체 버전. 교무부 ⑤예상문제 출제의 자동화 엔진이며, 같은 파이프라인으로 다른 연도·9월·수능·**부교재**·다른 **문제 유형**까지 확장하도록 설계. 2026-06-10 제목추론 144문항 → **2026-06-12 기준 bank.json 약 2,036문항**(모의고사 6유형 8시험 + 부교재 971[minute70 511·첫단추 460·N기출 86] + 26수능·25수능 일부). 신규 4유형(주제·요약·함의추론·어법) 출제원리 분석 완료 → **10유형 체계**로 확장. **2026-07-04 기준 bank 약 6,545문항; 전수 QA(결정적 게이트 ↔ AI 스윕)로 품질 정화 중(게이트 HARD 0 · 스윕 34%).**

## Context

- **목적**: 평가원 출제원리([[평가원_21유형_출제원리]])를 역산해 기출 지문으로 변형 문제를 대량 생성 → 학원 내부 수업·숙제용 자료. 본사 관점에선 "이식 가능한 콘텐츠 공장"(→ [[이식성_체크리스트]]).
- **데이터 위치**: 코드·중간산출물 전부 `C:\모의고사자동화`. 원천 지문 = Google Sheets DB Q시트(모의고사 ingest 결과, → [[내신_분석_시스템]]).
- **현황**: 제목추론만 데이터 존재(144). picker는 유형 무관 스키마라 다른 유형 추가 시 그대로 수용.
- **향후**: ①유형 6종 추가 ②부교재·타 연도/9월/수능 등록 ③**Netlify 외부 공개**(정적 프론트 + 클라이언트 docx 생성 검토).

## Details

### 파이프라인 (재현 순서)

| 단계 | 스크립트 | 입력 → 출력 | 핵심 |
|---|---|---|---|
| 1 추출 | `_hawe_q_extract.py` | DB Q시트 → `_hawe_q_all.json`·`_hq\q_NNN.json`·`_hq\_index.json` | 순서/삽입 **결정론 조립**, 빈칸 미리채움, 장문 (a~e)마커 제거, 어법/어휘/무관/장문에 **해설(교정 근거)** 첨부, 꼬리표 제거 |
| 2 생성+검증 | `_hawe_q_workflow.js` (Workflow) | `_hq\q_NNN.json` → 블록 | 문항별 파일 read(토큰 절약), 제목문제 출제→검증. **청크 12 + 재시도**(rate limit 회피) |
| 3 병합 | `_hawe_q_merge.py` | 실행 출력(들) → `_hawe_q_merged.json` | 인덱스 기준 커버리지 확인(144/144), `by_idx` 포함 |
| 4 정답 재배치 | `_hawe_q_rebalance.py` | merged → `_hawe_q_rebal.json` | 정답 위치 ①~⑤ 균등(4·4·4·3·3), 선지·해설 라벨 일괄 스왑(내용 불변) |
| 5 빌드 | `_hawe_q_build_all.py` (+ `_hawe_q_build.py`) | rebal → 시험별 8세트 docx/md | `build_munje_docx`(2단·출처 대괄호)·`build_ansol_docx`(정답표+해설), `strip_codes`로 A/B코드 제거, **인덱스-진실 normalize** |
| 6 렌더 | LibreOffice `soffice --convert-to pdf` | docx → pdf | exam_server `_libreoffice_convert`와 동일 방식 |
| 7 뽑기(로컬) | `hawe_picker.py` (Flask:5057) + `하위큐_뽑기.bat` | `_hawe_q_rebal.json` → 웹 picker | 시험·유형·검색 필터→선택→문제지+정답해설 docx+pdf ZIP |
| 8 문제은행 | `_hawe_q_bank.py` | rebal → `C:\하위큐웹\bank.json` | 정적 웹용 144문항 JSON(유형·출처 무관 스키마) |
| 9 뽑기(외부) | `C:\하위큐웹\` (정적, Netlify) | `bank.json` → 브라우저 docx | AINGEL식 빌더(필터·담기·자동담기), **브라우저 docx 생성**(docx.js, 백엔드 0). HAWE 디자인. 배포=GitHub→Netlify |

### 핵심 교훈 (반드시 지킬 것)

1. **★`지문_복원`은 고쳐진 지문이 아니다** — `지문`의 복사본이라 어법/어휘 정답 위치의 *일부러 틀린 단어*(예 low↔high, confuse↔help, having→have)가 그대로 있다. 그냥 쓰면 오류 박힌 지문. → **해설(=정답으로 고쳐진 한글 뜻)을 근거로 교정**해야 함. (원장 발견)
2. **순서/삽입은 LLM에 맡기지 말고 결정론 조립** — 답 순서 코드(①=ACB…)·삽입 위치로 코드가 미리 합쳐 마커 제거. LLM 순서 오류 방지.
3. **대량 동시 실행 = rate limit** — 288 에이전트 한 번에 → "API Error: Rate limited" 132건. **청크(≈12) + 재시도**로 회피. 실패분만 재실행 후 병합.
4. **에이전트 출력 신뢰 금지, 인덱스(파일명)=진실** — 헤딩을 `### N번` 대신 `**N번**`으로 쓰거나 출처를 틀리게 적는 경우 있음 → `_index.json`으로 N·출처 강제 교정(normalize).
5. **LLM 정답 위치 편향** — 정답이 ②③에 몰리고 ⑤ 거의 없음 → 5단계 후 재배치 필수.
6. 콘솔 한글 인코딩 → 스크립트는 `sys.stdout.reconfigure(encoding='utf-8')`.

### 신규 등록 절차 (다른 시험·부교재 추가 시)

1. 해당 지문을 DB Q시트에 ingest (→ 모의고사 ingest 스킬 / 부교재 ingest).
2. `_hawe_q_extract.py`의 `EXAMS`(또는 소스 목록)에 시험 ID 추가 → 실행.
3. `_hawe_q_workflow.js` 실행(`{dir, count}` 또는 `{dir, indices}`).
4. merge → rebalance → build_all → render.
5. picker는 `_hawe_q_rebal.json` 재로드(서버 재시작)만 하면 신규 문항 노출.

### 산출물 (2026-06-10)

- 8세트 = `하위큐_제목추론_{YY}-06_{고N}_문제지.{docx,pdf}` + `_정답해설.{docx,pdf,md}` (`C:\모의고사자동화`).
- 검증: 마커·빈칸·코드 잔존 0, 정답 균등, 틀린 단어 교정 확인.

### 2026-06-12 세션 업데이트 (대량 생성 + 웹 개선 + 신규유형 분석)

**파이프라인 진화**: 구 `_hawe_q_workflow.js`(제목 전용) → **`_hq5_workflow.js`**(5/6유형 범용, RULES에 유형별 출제원리, 청크+재시도+세션한도 isFatal 가드). 병합=`_hq5_merge.py`(무결성검사), 빌드=`_hawe_q_bank.py`(제목+5유형+부교재→`C:\하위큐웹\bank.json`). 지문 소스: 클린지문 `{dir}\p_NNN.json` (부교재=hwpx/PDF 추출, 모의고사 고3=Q시트 `지문_복원` 컬럼 gspread 추출, 26-06 고3 빈칸/순서/삽입은 시트 구조화컬럼서 복원).

**생성 현황**: 모의고사 5유형 8시험(23~26 6월 고1·고2, 각 ~80문항) + 부교재 971(minute70 5회·첫단추 미니/유형·N기출 미니3·4) + 26수능 87·25수능 부분. 고3 18시험 지문 추출완료(`_hq3`), 9월 고1·고2 23~25 + 11월 일부 추출완료(`_hq3b`).

**★대량 생성 함정**: ①워크플로우 동시 3개 실행 → **서버 rate limit**(동시 에이전트 과다). **한 번에 하나씩** 실행으로 회피. ②대형 배치(~100+ jobs)가 **출력 0바이트로 조용히 사망**하는 경우 → 트랜스크립트에서 `_hq5_recover.py`로 블록 복구 후 병합. ③`_hq5_merge` dedup은 기존 항목 유지 → 결함 재생성 반영하려면 `_cd_fixdefects.py drop` 선행.

**웹(app.js) 대폭 개선**: 2단 신문식 흐름(keepNext/keepLines 제거 — Word에서 칼럼 비는 버그 + 화면 ■마커 제거) · **한 단에 한 문제씩** 체크박스(ColumnBreak) · **문제+답지 한 파일**(combinedDoc, 답지 새페이지) · **유형별 정렬**(순서·삽입·주제·빈칸·어휘·어법) · **부교재 학년 스킵**(학년 무관) · **교재→회차 종속** · **자동 전체선택 해제**(직접 선택) · **선택목록에 고른 유형 표시** · 무관 지문 양쪽정렬 줄바꿈 수정. ⚠️포트 **5070은 SIP라 크롬 차단(ERR_UNSAFE_PORT)** — 5070(preview)/5058(.bat) 사용. 캐시: `index.html`의 `app.js?v=` 버전 올리고 **하드 새로고침**.

**신규 4유형 출제원리 (실물 평가원/수능 분석 기반, → 상세 `_haweq_next_spec.md`)**:
- **주제**: 선지=`[내용 추상명사 head]+of/관계사+[소재]`. 내용어 **극성**(±/중립)·**단복수**(복수=나열형/단수=단일초점)로 글 역추론. 오답=부분진술·과대·극성역전·날조·단복수불일치. 해설=틀린단어 **빨간 취소선+(반대/무관)**.
- **요약**: 한문장+(A)(B)쌍. 오답 핵심=**한쪽만 맞는 함정**(정답 칸 공유)+극성역전+유사어+관계왜곡.
- **함의추론**: **선별 출제**(비유·관용 핵심구 지문만, 부적합 skip). 정답=(밑줄 속뜻)∩(논지). 오답=표면오독·역전·관계왜곡·부분·무관·극단 +소재어 재사용. (수능23~26 밑줄구 전부 은유 확인)
- **어법**: 구조형:해석형 ≈ 4:6(모의고사 트렌드=해석형). 해석형=대동사·대명사지칭·능수동·수식어구분리수일치·의미기반병렬. 강한 문법검증 필수.
- **지문변형 엔진**(대안A 3단계): L2 적극 구문변형(태·강조·도치·분사구문·구↔절)=주제/제목/무관/빈칸/순서/삽입, L1=어법/어휘. 목적=원문암기 방지+학교변형 모방. 가드레일=문법정확·의미동일·정답근거 신호어 보존.

**다음 작업 큐**(토큰 리셋 후, → `_haweq_next_spec.md`): ①신규4유형 룰 구현 ②지문변형 엔진 ③23·24·25-09 고1·고2 + 26-06 고3 **10유형 생성** ④수능·학평 고3·부교재 결함·어휘 해설 백필.

### 2026-06-13 세션 (메가스터디 Opus 직접생성 + 운영 교훈)

워크시트메이커에서 추출한 **메가스터디 N제 고1영어 229제 3·5·8과 28지문**(본문+해석, Claude-in-Chrome로 textarea 추출 → 다운로드) → **10유형 267문항** 생성·등록. bank 부교재 출처(`book='메가스터디 N제 고1영어 229제'`, `round=과(페이지범위)`, **id 3000000+** 격리), 로컬 5058 배포. 작업물 전부 `C:\하위큐웹\_mega_gen\`, 빌드 `_megastudy_*.py`.

- **헤르메스 대신 Opus 4.8 직접 생성**(원장 선택, API비용 0): 첫 지문 10유형을 골든(`_gold_example.json`)으로 만들고 → **서브에이전트(general-purpose, model='opus') 지문별 병렬**(`_PROMPT.md` read) → `q_<key>.json`. **코드 검증 게이트**(어휘/어법 `①{}`×5, 무관 맨동그라미≥5, 삽입 `(①)`×5+박스, 순서 ABC, choices 마커형=[], ans/ansIdx)로 전수 점검 — 270문항 형식오류 0. 적합도 게이트=함의추론 비유구 없으면 `skip`(28중 13 skip).
- **★정답 쏠림(치명 품질결함)**: 골든의 고정 정답위치(빈칸①·제목①·어휘⑤·무관③·어법④…)를 28지문이 그대로 모방 → 전체 정답 **①이 127개**로 쏠림. 해결:
  - **선지형**(제목·주제·빈칸·요약·함의·순서·무관)=**프로그램 재배치(내용 100% 보존)** — 선지 배열에서 정답 위치만 이동, 순서=(A)(B)(C) **라벨 재배치**(읽기순서 보존), 무관=무관문장 위치 이동(나머지 순서 유지). `_megastudy_rebalance.py`.
  - **마커형**(어휘·어법·삽입)=마커 위치가 곧 정답이라 셔플 불가 → **목표 정답(`_targets.json`)을 주고 에이전트 재생성**(`_PROMPT_REBAL.md`).
  - 순서·삽입은 **①을 드물게**(각 2개). 최종 전체 ①42·②53·③59·④59·⑤54.
- **★해설 stale 버그**: 정답 위치를 바꾸면 해설이 옛 번호를 참조(제목 ans②인데 "→①", 순서 ③인데 "=④") → **정답/선지 위치를 바꾸면 해설도 반드시 동기화**. + 품질도 모의고사가 기준(`- 정답 N:` 시작·논지요약·**영어 근거구 인용**·오답 5개 **개별** 분석(반대/무관/부분/과대/날조))인데 메가는 짧고 뭉뚱그림 → 모의고사 해설 추출(`_gold_expl.json`)·**267개 전부 재작성**(`_PROMPT_EXPL.md`, id별 expl 교체). 평균 150→**735자**, stale 0.
- **★★bank.json 재빌드가 외부 추가 문항을 삭제(치명)**: `_hawe_q_bank.py`(+portable/sandbox)가 `_hawe_q_rebal`+`_hq5`+부교재로 **bank를 통째 재생성(id 0부터)** → 세션 중 메가 267이 통째 증발(2855→2588). 해결=**빌더 3종 모두 빌드 끝에 `_mega_gen/_FINAL_mega_items.json`(메가 canonical 백업)을 자동 병합**(id 3000000+ 충돌가드). **교훈: bank.json 직접 추가만으론 재빌드에 날아간다 — 외부 추가 문항은 별도 canonical 파일 + 빌더 병합이 원칙.** 복구는 items_*+expl_*에서 `_megastudy_restore.py`.
- **렌더 룰 신규**(app.js, v=ai26): ①**함의추론 밑줄** — 지문 지칭구를 `{{구}}` 마커로 표시 → `fmtPassage`/`passageRuns`가 `<u>` 렌더(골든 예시에 밑줄이 없던 게 누락 원인). ②**요약문 위치** — `박스`는 유형별: **삽입=지문 위, 요약=지문 아래**(preview·munje 분기). (어휘·어법 `①{word}` 밑줄은 기존)
- **함정**: 셸로 한글 문자열 비교 시 인코딩 깨져 **0건 오탐**(파일은 멀쩡) — 검증은 PYTHONUTF8 파이썬으로. Claude-in-Chrome JS 반환이 콘텐츠필터에 막히면(`[BLOCKED]`) Blob 다운로드로 우회.

### 2026-07-01~03 세션 (수특 Light영어 전권 + 무관오염 정리 + 배포)

**수특 Light영어 하위큐 전권 완성**: 01~23강+TEST **22세트 1,207문항**. 한줄해석 학생용 PDF(아잉카, 공유폴더 `\\desktop-07gd04s`) → `_stl_extract.py`로 지문 추출(▶헤더로 지문 분리, ❶❷ 원문자 제거 후 영어 합본, 무단도용/EBS/페이지번호 꼬리표 제거). 강별 유형=유형편(01목적·02심경·03요지·04함축·05제목·06도표·07일치·08어법)·주제소재편(17~22강). bank 6,682문항, verdant-unicorn/haweq 배포.

**★무관 오염 원인+식별법 (다른 EBS 교재에도 재사용)**: EBS "무관한 문장 찾기" 유형 지문은 원문에 무관 문장이 baked돼 있어, 그 지문으로 제목·어휘·빈칸 등 **다른 유형**을 만들면 무관 문장이 오염된 채 들어간다.
- **식별**: 한줄해석 PDF 각 지문 header `▶출처 [유형]`을 파싱(`_stl_typemap.py`)해 "무관한 문장 찾기" 유형 지문을 특정(수특 Light영어=11강 전체·18강N2·20강N1·21강N0·22강N4·TEST N17).
- **무관 문장 확정**: 식별→적대검증 2단계 Workflow(pipeline). ⚠️Workflow `schema` **property 키는 영문 필수**(한글 키=API 400). 11강N1="A person might present…"가 원장이 준 예시와 정확히 일치해 엔진 신뢰성 검증됨.
- **★발견**: 한줄해석 원문(`_stl` 재추출)은 **대부분 이미 클린**(아잉카가 무관 제거)이고, 오염은 **이전 scope 생성분**에 있었다. → `_stl` 무관 잔존분(11강N0·21강N0·22강N4)만 문장 제거 + scope 오염 9지문 삭제 + 클린 재생성. TEST N17(확신 low)=지문 5문장 일관해 오검출(원래 클린).

**어법 정답 유형 분산 (편중 해소)**: 골든 mimic처럼 어법 정답이 한 유형에 쏠림(15강 6/6 분리수일치·16강 6/6 분사) → `_hq5_workflow.js` RULES['어법']에 **"정답 유형 분산(필수)"** 추가. 분리수일치는 오답보기로 자주 깔되 정답은 대동사·분사 능수동·관계사 등과 번갈아. 재생성 결과 4유형 분산 확인. (상세 룰=메모리 `rule_eobeop_chuljae`)

**순서 재검증 게이트 (배포 전 필수)**: AI 순서생성이 일부 깨지므로(합본≠원문/단락 불연속) 생성 후 `_seq_verify.broken_keys()`로 확인 → `_seq_gen build`+`_seq_apply`로 결정적 재생성(24→0, 8→0). → [[하위고사_모의고사제작]]의 순서 결정론 원칙과 동일.

**06강·리딩파워 삭제**: 06강(도표/실용문)=변형 부적합 7문항 삭제. 리딩파워 CH1~6=안 쓰기로 하위큐에서 완전 삭제(bugyo 133 + status suteuk 아래 readingpower 블록 + 요약 for루프 하드코딩). 백업 `.bak_*`.

**이름규칙**: exam=`수특 Light영어 NN강`, 23강=`수특 Light영어 TEST`. `_stl_extract`·`_hq5_status_data`(suteuk 필터·라벨·요약루프) 함께 수정. ⚠️exam 이름 바꾸면 status_data 매칭이 깨지니 3곳(필터·블록·요약루프) 동시 수정.

### 2026-07-04 세션 (전수 QA — 결정적 게이트 ↔ AI 스윕)

**목표**(원장 지시): 중복 제거 + 전 문항(bank 6,545) 품질 전수검사, **"정말 하나도 오류가 없을 때까지"** 게이트↔스윕↔수정 루프. 모든 수정은 **소스파일에만 반영, 라이브 `bank.json`은 미반영**(배포는 원장 허락 후). 백업 다수(`.bak_*`).

**★QA 아키텍처 (재사용 가능한 두 축 분리)**:
- **결정적 게이트**(`_qa_gate.py`, 코드·전수·빠름): 유형별 형식·무결성 하드체크 → 통과 시 형식결함 0. 학교시험(origin=학교, 실제 기출)은 변형 QA 대상 아니라 스킵. 검사 항목: 정답형식·마커5(어휘/어법 `①{}`·무관 맨동그라미·삽입 `(①)`)·순서 (A)(B)(C)·요약 (A)(B)·함의 밑줄·빈칸 + **EXPL_META**(해설에 제작 메타/SKIP노트/영문 파이프라인 잔재)·**SEQ_EXPL**(순서 해설 정답번호·배열 ↔ ans)·**SEQ_LABEL**(순서 해설 오답배제의 선지 라벨 오지칭, **번호↔배열 양방향**)·**INS_ADJ**(삽입 마커 인접=빈 슬롯). 리포트 `_qa/gate_report.json`.
- **AI 스윕**(`_qa/hq_sweep.js`, 에이전트·의미론·샘플): 배치파일(6문항)을 **에이전트가 직접 Read**(내 컨텍스트 절약)해 blind 재검사 — 정답타당성·복수정답/무정답·해설정합·유형일치·지문오염·가짜변형(VOCAB/GRAMMAR_FAKE). 순서는 프로그램 검증분이라 재풀이 않고 형식만. args `{start,end}`.
- **루프**: 스윕 발견 → **규칙화 가능한 반복 패턴은 게이트로 흡수**(전수·결정적 탐지, AI 샘플링 누락 방지) → 순수 의미론만 AI 개별수정(`_qa/hq_fix5.js`=순서해설 재작성/어휘 트랩 재조립) 또는 결정적 스크립트. 반영=`_qa_apply_fix.py`(fix_jobs 조인·소스별 반영·이중래핑 언랩) → 재빌드(`_hawe_q_bank.py`) → 재게이트.

**처리 결과** (게이트 HARD 0 달성, bank 6,545):
- **INS_ADJ(삽입 빈 슬롯 `( ④ )( ⑤ )`) 39건**: 원문뱅크(passage_bank) 대조로 원문 문장수 실측 → 원문 6문장+ 확보 **3건 재생성 부활**(원문 복원 후 given 빼고 5선지 재배치), 원문 4~5문장이라 삽입 5선지가 **물리적으로 불가능한 36건은 삽입부적합으로 제외** + 빌더 끝에 인접마커 스킵 필터(재발 자동차단).
- **순서 해설 22건 재작성**: 정답·순서 논증은 전부 정확, **오답배제 문장이 선지 라벨을 오지칭**(`⑤처럼 (C)-(A)-(B)`인데 ⑤=(C)-(B)-(A))하거나 보일러플레이트 → SEQ_LABEL 게이트로 전수 흡수 후 ans 고정 재작성(오답배제는 정확 라벨만, 불확실하면 생략).
- 어휘 트랩 재조립 1(정답어가 문맥상 정방향=NO_ANSWER → 반의어로 실제 오답 조립) · 텍스트 오염 7(telling them·diet improved·고아조각 등, 동일지문 타문항과 대조로 원문 확정) · 무관 파편 재마킹 1(`Period.` 단독 ⑤ → 도입문 1개를 ①로 승격해 5선지 복원) · 제작메타 청소 1.

**진행/인수인계**: AI 스윕 배치 **1~368 / 1091 완료(34%)**. 남은 **pending 7건**(`_qa/pending_issues.json`: 어휘 트랩 재조립 4·해설 교정 2·삽입 정답 재설정 1) + 스윕 369~1091. 재개=pending 처리 → `Workflow hq_sweep.js {start:369,end:518}`. 진행 스냅샷 `C:\모의고사자동화\_qa\PROGRESS.md`.

**★교훈**:
1. **삽입/무관 유형 적합성은 원문 문장수가 결정** — 삽입은 원문 6문장+(본문 5)여야 5선지를 인접 없이 배치, 무관도 후보 5문장+ 필요. 짧은 지문에 억지 생성하면 빈 슬롯·파편 마커가 남는다. 재생성/제외 판정은 [[원문마스터뱅크_재편_2026-07|passage_bank]] 문장수 실측으로.
2. **반복되는 AI 발견 패턴은 게이트로 흡수** — 제작메타 누출·순서 라벨 오지칭처럼 규칙화 가능한 결함은 정규식 게이트로 옮겨 전수·결정적 탐지(AI 샘플링은 반드시 누락 발생). AI 스윕은 게이트가 못 잡는 순수 의미론만 담당하도록 역할 분리.
3. **Workflow `args`는 문자열로 전달됨** — 스크립트 상단 `typeof args==='string'?JSON.parse(args):args` 방어 필수(누락 시 `.map` 실패·구조분해 undefined로 0건 조용히 통과). 워크플로 output은 `{result:{result:[...]}}` 이중래핑 → 언랩 필요.
4. **에이전트 반환 신뢰 주의** — 삽입 adjudicate가 `passage` 필드에 `</passage>`·해설·SKIP노트를 통째로 흘려 지문을 오염시킨 사례. 구조화 출력이 지문에 새면 게이트 MARK5로 포착됨.
5. **rate limit** — 150 에이전트 동시 스윕 시 후반부가 "Server temporarily limiting" rate limited. 150배치씩 분할 + 실패분 재시도로 회피.

## Links

- [[수능_유형별_문제구조_제작규칙]] — 유형별 구조·마커·정답균형·해설 규칙(이 세션 교훈 반영)
- [[평가원_21유형_출제원리]] — ★수능 21유형 출제원리 마스터(이 엔진의 생성 근거, 제목 A1~A6·오답 B1~B6 심화 포함)
- [[선지_오답구성_원리]] — 변형 시 오답 선지 구성 공식(빈칸·주제·제목·요약)
- [[2_교무부]] ⑤예상문제 출제 — 이 시스템이 그 자동화 엔진
- [[5_시스템개발부]] — 파이프라인·picker 유지보수 주체
- [[문제DB_공유_데이터베이스]] — ★원천 문제 DB(모의고사 제작·내신 파이널과 공유). 변형의 재료
- [[하위고사_모의고사제작]] · [[하위파이널_내신제작]] — 같은 DB 공유 자매 계열(하위큐는 둘 다에 쓰여 **수능+내신 출제원리 모두 반영**)
- [[서술형_출제원리_분석]] — 내신 출제에 쓸 때의 출제 이해(수능측은 [[평가원_21유형_출제원리]])
- [[내신_분석_시스템]] — 원천 지문 코퍼스·출처 매칭
- [[이식성_체크리스트]] · [[내신_자동생성_파이프라인]] — 이식·자동화 자산 계열
